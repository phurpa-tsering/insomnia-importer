{
  "name": "@voiden/plugin-insomnia-importer",
  "version": "1.1.0",
  "type": "module",
  "private": false,
  "description": "Import Insomnia v4 (.json) and v5 (.yaml/.json) collection exports into Voiden — preserves folder structure, headers, query params, request bodies, and authentication.",
  "main": "./src/plugin.ts",
  "scripts": {
    "build": "node build.mjs && node build-main.mjs",
    "build:main": "node build-main.mjs",
    "zip": "node zip.mjs",
    "release": "node build.mjs && node build-main.mjs && node generate-manifest.mjs"
  },
  "peerDependencies": {
    "@voiden/sdk": ">=1.0.10",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@voiden/sdk": "latest",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
